export enum AuthProvider {
  LOCAL = 'local', // Autenticación tradicional (email/contraseña)
  GOOGLE = 'google', // Autenticación con Google OAuth
  FACEBOOK = 'facebook', // Autenticación con Facebook OAuth
}
